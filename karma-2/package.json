{
  "scripts": {
    "test": "karma start karma.conf.js --log-level debug --single-run",
    "tdd": "karma start karma.conf.js --log-level debug"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "karma": "^1.7.1",
    "karma-coverage": "^1.1.1",
    "karma-phantomjs-launcher": "^1.0.4",
    "phantomjs-prebuilt": "^2.1.15"
  }
}
